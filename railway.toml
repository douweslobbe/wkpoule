[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma migrate resolve --applied 20260101000000_init; npx prisma migrate resolve --rolled-back 20260602000003_drop_stale_prediction_unique; npx prisma migrate deploy && npx tsx prisma/seed.ts && HOSTNAME=0.0.0.0 node .next/standalone/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
