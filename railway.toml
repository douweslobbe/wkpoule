[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma migrate resolve --applied 20260101000000_init; npx prisma migrate deploy && npx tsx prisma/seed.ts && HOSTNAME=0.0.0.0 node .next/standalone/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
